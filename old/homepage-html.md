# book stack and book card setup

div (.book-stack)
    section (.book-card)
        img (.book-image)
        div (.textbox)
            h4 (book title (example: "Laws of UX"))
            h4 (subtitle (example: "Using Psychology to Design Better Products & Services"))
            p (author (example: "by Jon Yablonski"))
        div (.button-wrapper)
            a (.button.read-button)


